# Strapi Configuration
VITE_STRAPI_URL=http://localhost:1337
VITE_STRAPI_API_TOKEN=your_api_token_here

# Fallback to mock data when Strapi is not available
# Set to 'true' to always use mock data, or leave unset to use mock data only when Strapi fails
VITE_USE_MOCK_DATA=true